# Power Automate endpoints — never commit this file
# Copy to .env.local and fill in values

# CommandIQ chat agent (CommandIQ.tsx)

# VITE_COPILOT_ENDPOINT and VITE_RAMMAS_FLOW_URL removed — both flows now
# invoked via Power Apps SDK (see _1Service and Rammas_Send_ResponseService)
